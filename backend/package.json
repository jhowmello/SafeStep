{
  "name": "safestep-backend",
  "version": "1.0.0",
  "description": "JSON Server - API REST do SafeStep",
  "scripts": {
    "start": "node server.js",
    "start:network": "node server.js --network",
    "start:json-server-only": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.4",
    "express": "^4.22.1",
    "cors": "^2.8.6"
  }
}
